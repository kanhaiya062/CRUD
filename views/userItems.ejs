<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Item List</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background-color: #f4f4f4;
        }
        h1 {
            color: #2c3e50;
            text-align: center;
        }
        .item-list {
            list-style-type: none;
            padding: 0;
        }
        .item {
            background-color: #fff;
            border-radius: 5px;
            padding: 15px;
            margin-bottom: 15px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        .item a {
            color: #3498db;
            text-decoration: none;
            font-weight: bold;
        }
        .item a:hover {
            text-decoration: underline;
        }
        .rating {
            color: #e67e22;
            font-size: 0.9em;
        }
    </style>
</head>
<body>
    <h1>All Items</h1>
    <ul class="item-list">
        <% items.forEach(item => { %>
            <li class="item">
                <a href="/items/<%= item._id %>">Click here<br><%= item.name %></a>
                <p class="rating">Average Rating: <%= item.rating.toFixed(1) %> (<%= item.ratingCount %> ratings)</p>
            </li>
        <% }); %>
    </ul>
    <a href="/" class="back-link">← Back to Home</a>
</body>
</html>